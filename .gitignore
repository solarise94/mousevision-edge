# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
.venv/
venv/
env/
dist/
build/
*.egg-info/
.eggs/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# OS / editor
.DS_Store
.idea/
.vscode/

# 运行时产物（数据、上传、调试输出）
output/
tmp_frames/
tmp_ocr_acceptance/
tmp_lcd_debug/
tmp_eval/
tmp_gemini_weigh/

# 误落在仓库根目录的本地 manifest 碎片（正式产物在 run_*/manifest.json）
/manifest.json

# 工具本地缓存
.zcode/
*.mp4.bak

# 参考视频：体积较大，需手动放置（见 README）
RefVideo/*.mp4

# 部署专用配置（含本地 token / 端口，不进仓库）
compose.deploy.yaml
*.container
!deploy/quadlet/*.container
*.service
!deploy/quadlet/*.service

# 本地凭证 / 环境
.env
.env.*
